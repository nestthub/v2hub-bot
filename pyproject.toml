[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[project]
name = "v2hub-bot"
version = "1.0.0"
description = "Telegram bot for managing V2Hub VPN subscriptions"
readme = "README.md"
license = { text = "MIT" }

authors = [
    { name = "nestt", email = "nestt@tutamail.com" }
]

requires-python = ">=3.11"

classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
    "Framework :: AsyncIO",
    "Environment :: No Input/Output (Daemon)",
]

keywords = [
    "telegram",
    "bot",
    "vpn",
    "subscription",
    "v2hub",
    "aiogram",
]

dependencies = [
    "aiogram>=3.13.1,<4.0",
    "asyncpg>=0.30.0,<1.0",
    "pydantic-settings>=2.6.1,<3.0",
    "sqlalchemy[asyncio]>=2.0.36,<3.0",
    "v2hub>=1.0.8",
    "v2hub-admin>=1.0.3",
]

[project.optional-dependencies]
dev = [
    "aiosqlite>=0.22.1",
    "mypy>=1.5.0",
    "pre-commit>=3.4.0",
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.1.0",
    "pytest-mock>=3.11.0",
    "respx>=0.23.1",
    "ruff>=0.1.0",
]

[project.scripts]
v2hub-bot = "v2hub_bot.main:cli"

[project.urls]
Homepage = "https://github.com/nestthub/v2hub-bot"
Documentation = "https://github.com/nestthub/v2hub-bot#readme"
Repository = "https://github.com/nestthub/v2hub-bot"
Issues = "https://github.com/nestthub/v2hub-bot/issues"


[tool.hatch.build.targets.wheel]
packages = ["src/v2hub_bot"]


[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src"]


[tool.ruff.lint]
select = [
    "E",
    "W",
    "F",
    "I",
    "B",
    "C4",
    "UP",
    "ARG",
    "SIM",
    "TCH",
    "PTH",
    "RUF",
]

ignore = [
    "E722",
    "E501",
    "B008",
    "ARG002",
    "RUF001",
    "RUF002",
    "RUF003",
]


[tool.ruff.lint.per-file-ignores]
"__init__.py" = [
    "F401",
    "F403",
]

"tests/*" = [
    "ARG001",
    "ARG002",
]


[tool.ruff.lint.isort]
known-first-party = [
    "v2hub",
    "v2hub_admin",
    "v2hub_bot",
]


[tool.mypy]
python_version = "3.11"

strict = true
warn_return_any = true
warn_unused_configs = true

disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true

no_implicit_optional = true

warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true

strict_equality = true

plugins = [
    "pydantic.mypy",
]


[tool.pytest.ini_options]
testpaths = [
    "tests",
]

asyncio_mode = "auto"

addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
]

markers = [
    "unit: Unit tests",
    "integration: Integration tests",
    "slow: Slow tests",
]


[tool.coverage.run]
source = [
    "v2hub_bot",
]

omit = [
    "*/tests/*",
]


[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if TYPE_CHECKING:",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "@abstractmethod",
    "@overload",
]
